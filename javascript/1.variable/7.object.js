let name = 'apple';
let color = 'red';
let display = '🍎';
console.log(name);
console.log(color);
console.log(display);

let watermelon = {
 name : '수박',
 color : 'green',
 display : "🍉",
};
console.log(watermelon);

// 수박은 객체, 이름은 워터멜론, 색은 그린, 보여질때는 이모지를 사용해 수박그림 표현. 
// log를 사용해 객체 자체를 표현할 수도 있고 하나하나씩 나타내고 싶으면 .을 사용해 key(속성?)을 찾아 값을 나타낼 수 있음. 

//연결된 그룹을 패키지화 시킬 수 있는 것이 객체
//단일한 값을 담을 수 있는 원시타입(primitive type) vs 연관된 데이터를 함께 묶어 보관가능한 객체(object)


